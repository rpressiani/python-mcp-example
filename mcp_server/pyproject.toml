[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP Server with SSE Transport"
dependencies = [
    "mcp>=1.0.0",
    "starlette>=0.27.0",
    "uvicorn>=0.22.0",
    "httpx>=0.24.0",
]
requires-python = ">=3.11"

[tool.pyright]
venvPath = "."
venv = ".venv"

