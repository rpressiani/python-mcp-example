[project]
name = "mcp-agent"
version = "0.1.0"
description = "MCP Agent Runner"
dependencies = [
    "mcp>=1.0.0",
    "openai>=1.0.0",
    "httpx>=0.24.0",
]
requires-python = ">=3.11"

[tool.pyright]
venvPath = "."
venv = ".venv"

